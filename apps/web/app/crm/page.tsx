'use client';import {useEffect,useState} from 'react';import AppShell from '../../components/AppShell';import {api} from '../../lib';import {Modal,StatusBadge,SearchBox} from '../../components/AdminUI';
const L=({children}:{children:React.ReactNode})=><span>{children}</span>;
export default function CRM(){const[tab,setTab]=useState<'leads'|'deals'|'activities'>('leads');return <AppShell title="CRM" subtitle="Leads، الصفقات، المتابعات، المكالمات والملاحظات"><div className="tabs">{[['leads','العملاء المحتملون'],['deals','الصفقات'],['activities','النشاطات والمتابعات']].map(([k,l])=><button className={`tab ${tab===k?'active':''}`} onClick={()=>setTab(k as any)} key={k}>{l}</button>)}</div>{tab==='leads'?<Leads/>:tab==='deals'?<Deals/>:<Activities/>}</AppShell>}
function Leads(){const[rows,setRows]=useState<any[]>([]),[edit,setEdit]=useState<any>(null),[q,setQ]=useState('');const load=()=>api('/crm/leads').then(setRows).catch(e=>alert(e.message));useEffect(load,[]);const fresh=()=>setEdit({status:'NEW'});const save=async()=>{try{await api(edit.id?'/crm/leads/'+edit.id:'/crm/leads',{method:edit.id?'PUT':'POST',body:JSON.stringify(edit)});setEdit(null);load()}catch(e:any){alert(e.message)}};return <><div className="toolbar"><SearchBox value={q} onChange={setQ}/><button className="btn" onClick={fresh}>+ عميل محتمل</button></div><div className="card table-wrap"><table className="table"><thead><tr><th>الاسم</th><th>الشركة</th><th>المصدر</th><th>الحالة</th><th>المالك</th><th/></tr></thead><tbody>{rows.filter(r=>JSON.stringify(r).toLowerCase().includes(q.toLowerCase())).map(r=><tr key={r.id}><td>{r.name}</td><td>{r.company||'—'}</td><td>{r.source||'—'}</td><td><StatusBadge>{r.status}</StatusBadge></td><td>{r.owner?.name||'—'}</td><td><button className="btn secondary" onClick={()=>setEdit({...r})}>تعديل</button> <button className="btn danger" onClick={async()=>{if(confirm('حذف العميل المحتمل؟')){await api('/crm/leads/'+r.id,{method:'DELETE'});load()}}}>حذف</button></td></tr>)}</tbody></table></div>{edit&&<Modal title={edit.id?'تعديل عميل محتمل':'عميل محتمل جديد'} onClose={()=>setEdit(null)}><div className="form-grid"><label>الاسم<input className="input" value={edit.name||''} onChange={e=>setEdit({...edit,name:e.target.value})}/></label><label>البريد<input className="input" value={edit.email||''} onChange={e=>setEdit({...edit,email:e.target.value})}/></label><label>الهاتف<input className="input" value={edit.phone||''} onChange={e=>setEdit({...edit,phone:e.target.value})}/></label><label>الشركة<input className="input" value={edit.company||''} onChange={e=>setEdit({...edit,company:e.target.value})}/></label><label>المصدر<input className="input" value={edit.source||''} onChange={e=>setEdit({...edit,source:e.target.value})}/></label><label>الحالة<select className="select" value={edit.status||'NEW'} onChange={e=>setEdit({...edit,status:e.target.value})}>{['NEW','CONTACTED','QUALIFIED','PROPOSAL','WON','LOST'].map(x=><option key={x}>{x}</option>)}</select></label><label className="full">ملاحظات<textarea className="textarea" value={edit.notes||''} onChange={e=>setEdit({...edit,notes:e.target.value})}/></label><button className="btn full" onClick={save}>حفظ</button></div></Modal>}</>}
function Deals(){const[rows,setRows]=useState<any[]>([]),[edit,setEdit]=useState<any>(null);const load=()=>api('/crm/deals').then(setRows).catch(e=>alert(e.message));useEffect(load,[]);const save=async()=>{try{await api(edit.id?'/crm/deals/'+edit.id:'/crm/deals',{method:edit.id?'PUT':'POST',body:JSON.stringify({...edit,value:Number(edit.value||0)})});setEdit(null);load()}catch(e:any){alert(e.message)}};return <><div className="toolbar"><button className="btn" onClick={()=>setEdit({status:'OPEN',stage:'NEW'})}>+ صفقة</button></div><div className="card table-wrap"><table className="table"><thead><tr><th>العنوان</th><th>القيمة</th><th>المرحلة</th><th>الحالة</th><th>المالك</th><th/></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.title}</td><td>{r.value??0}</td><td>{r.stage}</td><td><StatusBadge>{r.status}</StatusBadge></td><td>{r.owner?.name||'—'}</td><td><button className="btn secondary" onClick={()=>setEdit({...r})}>تعديل</button> <button className="btn danger" onClick={async()=>{if(confirm('حذف الصفقة؟')){await api('/crm/deals/'+r.id,{method:'DELETE'});load()}}}>حذف</button></td></tr>)}</tbody></table></div>{edit&&<Modal title={edit.id?'تعديل صفقة':'صفقة جديدة'} onClose={()=>setEdit(null)}><div className="form"><label>العنوان<input className="input" value={edit.title||''} onChange={e=>setEdit({...edit,title:e.target.value})}/></label><label>القيمة<input className="input" type="number" value={edit.value||0} onChange={e=>setEdit({...edit,value:Number(e.target.value)})}/></label><label>المرحلة<input className="input" value={edit.stage||'NEW'} onChange={e=>setEdit({...edit,stage:e.target.value})}/></label><label>الحالة<select className="select" value={edit.status||'OPEN'} onChange={e=>setEdit({...edit,status:e.target.value})}><option>OPEN</option><option>WON</option><option>LOST</option></select></label><button className="btn" onClick={save}>حفظ</button></div></Modal>}</>}
function Activities(){const[rows,setRows]=useState<any[]>([]),[edit,setEdit]=useState<any>(null);const load=()=>api('/crm/activities').then(setRows).catch(e=>alert(e.message));useEffect(load,[]);const save=async()=>{try{await api(edit.id?'/crm/activities/'+edit.id:'/crm/activities',{method:edit.id?'PUT':'POST',body:JSON.stringify(edit)});setEdit(null);load()}catch(e:any){alert(e.message)}};return <><div className="toolbar"><button className="btn" onClick={()=>setEdit({type:'FOLLOW_UP'})}>+ نشاط</button></div><div className="card table-wrap"><table className="table"><thead><tr><th>النوع</th><th>الموضوع</th><th>الموعد</th><th>مكتمل</th><th/></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.type}</td><td>{r.subject}</td><td>{r.dueAt?new Date(r.dueAt).toLocaleString('ar'):'—'}</td><td>{r.completedAt?'نعم':'لا'}</td><td><button className="btn secondary" onClick={()=>setEdit({...r})}>تعديل</button> <button className="btn danger" onClick={async()=>{if(confirm('حذف النشاط؟')){await api('/crm/activities/'+r.id,{method:'DELETE'});load()}}}>حذف</button></td></tr>)}</tbody></table></div>{edit&&<Modal title={edit.id?'تعديل النشاط':'نشاط جديد'} onClose={()=>setEdit(null)}><div className="form"><label>النوع<input className="input" value={edit.type||''} onChange={e=>setEdit({...edit,type:e.target.value})}/></label><label>الموضوع<input className="input" value={edit.subject||''} onChange={e=>setEdit({...edit,subject:e.target.value})}/></label><label>الوصف<textarea className="textarea" value={edit.body||''} onChange={e=>setEdit({...edit,body:e.target.value})}/></label><label>الموعد<input className="input" type="datetime-local" value={edit.dueAt?String(edit.dueAt).slice(0,16):''} onChange={e=>setEdit({...edit,dueAt:e.target.value||null})}/></label><label className="check"><input type="checkbox" checked={!!edit.completedAt} onChange={e=>setEdit({...edit,completedAt:e.target.checked?new Date().toISOString():null})}/> مكتمل</label><button className="btn" onClick={save}>حفظ</button></div></Modal>}</>}
