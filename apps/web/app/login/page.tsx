'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';import {api} from '../../lib';
export default function Login(){const[identifier,setIdentifier]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState('');const r=useRouter();async function go(e:any){e.preventDefault();setError('');try{const d=await api('/auth/login',{method:'POST',body:JSON.stringify({identifier,password})});localStorage.setItem('alghaly_token',d.token);r.push('/dashboard')}catch(err:any){setError(err.message||'تعذر تسجيل الدخول')}}return <main className="login"><form className="loginbox" onSubmit={go}><div className="logo"><span>Al</span>Ghaly</div><div className="side-title" style={{padding:'0'}}>منصة إدارة الأعمال المتكاملة</div><h1>تسجيل الدخول</h1><p className="muted">أدخل بيانات حسابك للوصول إلى لوحة التحكم.</p><label>البريد الإلكتروني أو رقم الهاتف<input className="input" type="text" value={identifier} onChange={e=>setIdentifier(e.target.value)}/></label><label>كلمة المرور<input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<div className="error">{error}</div>}<button className="btn">دخول آمن</button></form></main>}
