'use client';import {useEffect,useState} from 'react';import AppShell from '../../components/AppShell';import {api} from '../../lib';
export default function Reports(){const[d,setD]=useState<any|null>(null);useEffect(()=>{api('/reports/summary').then(setD).catch(e=>alert(e.message))},[]);const exportCsv=()=>{if(!d)return;const rows=Object.entries(d).map(([k,v])=>`${k},${JSON.stringify(v)}`);const blob=new Blob([['metric,value',...rows].join('\n')],{type:'text/csv;charset=utf-8'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='alghaly-report.csv';a.click();URL.revokeObjectURL(a.href)};return <AppShell title="التقارير" subtitle="مؤشرات من قاعدة البيانات مع تصدير مجاني"><div className="toolbar"><button className="btn" onClick={exportCsv}>تصدير CSV</button><button className="btn secondary" onClick={()=>window.print()}>طباعة / حفظ PDF</button><button className="btn secondary" onClick={()=>api('/reports/summary').then(setD)}>تحديث</button></div><div className="kpi-grid">{[['المستخدمون','users'],['العملاء','clients'],['الموظفون','employees'],['المشاريع','projects'],['المهام','tasks'],['التذاكر','tickets'],['العملاء المحتملون','leads'],['طلبات التطبيقات','appRequests'],['الخدمات النشطة','services']].map(([l,k])=><div className="card" key={k}><div className="mini">{l}</div><div className="num" style={{fontSize:28,fontWeight:900,marginTop:7}}>{d?.[k]??'—'}</div></div>)}</div>{d&&<div className="grid2"><div className="card"><h3>إنجاز المشاريع</h3><div className="progress"><i style={{width:`${d.projectCompletion}%`}}/></div><p>{d.projectCompletion}%</p></div><div className="card"><h3>إنجاز المهام</h3><div className="progress"><i style={{width:`${d.taskCompletion}%`}}/></div><p>{d.taskCompletion}%</p></div></div>}<div className="notice" style={{marginTop:15}}>تصدير CSV والطباعة لا يحتاجان خدمة مدفوعة. اختر Save as PDF من نافذة الطباعة.</div></AppShell>}
