'use client';
import {useEffect,useState} from 'react';
import {api} from '../../lib';
function renderContent(c:any){if(!c)return null;if(typeof c==='string')return <p>{c}</p>;if(Array.isArray(c))return c.map((x,i)=><div key={i}>{renderContent(x)}</div>);if(c.blocks)return c.blocks.map((b:any,i:number)=><section key={i} className="card" style={{marginBottom:14}}><h2>{b.title||''}</h2><p>{b.text||b.content||''}</p></section>);return <pre className="code">{JSON.stringify(c,null,2)}</pre>}
export default function PublicPage(){const[p,setP]=useState<any>(null);const[loading,setLoading]=useState(true);useEffect(()=>{const slug=new URLSearchParams(window.location.search).get('slug')||'home';api('/public/pages').then(xs=>setP((xs||[]).find((x:any)=>x.slug===slug)||null)).catch(()=>{}).finally(()=>setLoading(false))},[]);if(loading)return <main className="public"><div className="public-section"><div className="card"><h1>جاري التحميل...</h1></div></div></main>;if(!p)return <main className="public"><div className="public-section"><div className="card"><h1>الصفحة غير موجودة</h1><p className="muted">تحقق من الرابط أو من نشر الصفحة داخل لوحة الإدارة.</p></div></div></main>;return <main className="public"><header className="public-nav"><div className="logo"><span>Al</span>Ghaly</div></header><section className="public-section"><h1>{p.title}</h1>{renderContent(p.content)}</section></main>}
