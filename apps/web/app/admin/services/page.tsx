'use client';import CrudPage from '../../../components/CrudPage';const j=(v:any)=>{if(!v)return null;try{return typeof v==='string'?JSON.parse(v):v}catch{return null}};export default function Services(){return <CrudPage title="الخدمات" subtitle="إضافة وتعديل وتفعيل وإيقاف كل خدمات Al Ghaly" path="/admin/services" fields={[{key:'name',label:'اسم الخدمة',required:true},{key:'slug',label:'Slug',required:true},{key:'description',label:'الوصف',type:'textarea',full:true},{key:'imageUrl',label:'رابط الصورة',type:'url'},{key:'duration',label:'مدة التنفيذ'},{key:'price',label:'السعر',type:'number'},{key:'sortOrder',label:'الترتيب',type:'number'},{key:'metadata',label:'بيانات إضافية JSON',type:'textarea',full:true},{key:'active',label:'الخدمة مفعلة',type:'checkbox'}]} columns={[{key:'name',label:'الخدمة'},{key:'slug',label:'الرابط'},{key:'duration',label:'المدة'},{key:'price',label:'السعر'},{key:'active',label:'الحالة'}]} normalizeCreate={v=>({...v,metadata:j(v.metadata)})} normalizeUpdate={v=>({...v,metadata:j(v.metadata)})}/>} 
